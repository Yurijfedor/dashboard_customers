import styled from "styled-components";

export const TitleWrapp = styled.div`
  display: grid;
  gap: 7px;
`;

export const TableTitle = styled.h2`
  font-weight: 600;
  font-size: 22px;
  line-height: 1.5;

  letter-spacing: -0.01em;

  color: #000000;
`;

export const TableTitleBottom = styled.p`
  font-weight: 400;
  font-size: 14px;
  line-height: 1.5;

  letter-spacing: -0.01em;

  color: #16c098;
  cursor: pointer;
`;
